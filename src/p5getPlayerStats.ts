interface Player {
    name: string,
    average: number,
    rank: string
}

interface player1 { 
    name: "Nova", 
    scores: [90, 85, 95, 80] 
}
function getPlayerStats(player: Player): Player{ 
    const scoresArray = player
} 